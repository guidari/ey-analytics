import { Box } from "@mui/material";

import { Layout } from "../components/layout";

const Users = () => {
  return (
    <Layout>
      <Box sx={{ backgroundColor: "salmon" }}>Users</Box>
    </Layout>
  );
};

export default Users;
