import { Box } from "@mui/material";

import { Layout } from "../components/layout";

const Dashboard = () => {
  return (
    <Layout>
      <Box>Dashboard</Box>
    </Layout>
  );
};

export default Dashboard;
